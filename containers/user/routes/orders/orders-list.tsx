import { Empty } from '@/components/user/empty';

export function OrdersList() {
  return (
    <section className="container h-full">
      <Empty text="سفارشی ثبت نکرده اید!" />
    </section>
  );
}
